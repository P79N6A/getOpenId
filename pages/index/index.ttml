<view class="boss">
    <!-- <view class="intro">Welcome to Mini Program</view> -->
    <view class="header">
        <image class="avatar" src="{{ user.avatarUrl }}" mode="aspectFill" />
        <view class="name"> {{ user.nickName }} </view>
    </view>
    <view class="content" style="height:{{ (phone.windowHeight-123)+'px' }};">
        <view class="intro">商超付款码：</view>

        <image class="barcode" src="{{ img.path }}" mode="aspectFit" />
    </view>
</view>